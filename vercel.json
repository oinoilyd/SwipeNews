{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/clustered-news.js": { "maxDuration": 45 },
    "api/generate-takes.js": { "maxDuration": 30 }
  },
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
